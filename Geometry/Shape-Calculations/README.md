This program provides geometric calculations for two different shapes: a rectangle and a circle. The user inputs the length and breadth for a rectangle and the radius for a circle. Based on these inputs, the program calculates:

Rectangle: Area and Perimeter.

Circle: Area and Circumference (using a constant value for Pi).

The output is formatted to display results with two decimal points for precision.